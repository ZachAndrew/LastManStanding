/**
 * Created by Zach on 10/02/2017.
 */

package andrew.zach.lmsprototype1;

import java.util.ArrayList;
import java.util.List;

public class Teams {

    //vars
    //Players
    public String Player1;
    public String Player2;

    int count=0;
    //String to hold player names
   // String array[]=new String[2];
    String tempString="temp";
    int x;

   // List<String> list= new ArrayList<>();

    //Default constructor
    public Teams(int order,String name) {
       //count=order;
       // tempString=name;


       // list.add(name);
        //list.add(name);
       // PlayerList myListOfPlayers= new PlayerList(order,name);
    }

    /*******ATTEMPT 1********
        //can add info of Team
        String array[]={this.Player1,this.Player2};
        count=order;
        x=new Integer(count);

     public Teams(int order,String name) {
     count = order;
     if (order == 0 || order == 1) {
     Player1 = name;

     if (order == 1) {
     Player2 = name;
     }
     }
     }


        //*****CHECKER***
        this.tempString=array[count];
        array[count]=name;
        //***************
        //array[order]=name;
      //  array[order]=name;
       // this.Player1=new String (name);
     //   String.format(Player1,name );
    **************************/





}







